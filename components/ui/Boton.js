import styled from '@emotion/styled';

const Boton = styled.a`
    display: inline-block;
    padding: 1.3rem 5rem;
    font-weight: 600;
    border-radius: 4px;

    &:hover {
        cursor: pointer;
    }
`;

export default Boton;